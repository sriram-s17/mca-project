from django.urls import path
from .views import *

urlpatterns = [
    # path('', sales),
    # path('add/', addsale),
    # path('<int:id>/addpayment/', addpayment, name="add_payment"),
    # path('<int:id>/', viewsale, name="view_sale"),
]